apiVersion: 1
datasources:
  - name: PostgreSQL
    type: postgres
    typeName: PostgreSQL
    access: proxy
    url: db
    user: postgres
    database: postgres
    typeLogoUrl: public/app/plugins/datasource/postgres/img/postgresql_logo.svg
    basicAuth: false
    isDefault: true
    jsonData:
     sslmode: disable
    secureJsonData: 
      password: Auique1_SP
    readOnly: false
    version: 1
    editable: true